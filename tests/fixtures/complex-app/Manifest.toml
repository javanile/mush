[package]
name = "complex-app"
version = "0.1.0"
edition = "2022"

[dev-dependencies]
#bats = "git https://github.com/sstephenson/bats.git v0.4.0"
bashunit = "mush test_with_bashunit *"

#[dev-dependencies-build]
#bats = "exec install.sh $MUSH_MANIFEST_DIR"

[features]
test_with_bashunit = true
